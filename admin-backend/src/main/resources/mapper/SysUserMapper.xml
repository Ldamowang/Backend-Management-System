<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iflytek.admin.modules.system.mapper.SysUserMapper">

    <select id="selectUserByUsername" resultType="com.iflytek.admin.modules.system.entity.SysUser">
        SELECT * FROM sys_user WHERE username = #{username} AND deleted = 0
    </select>

    <select id="selectUserRoles" resultType="java.lang.String">
        SELECT r.role_key FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId} AND r.deleted = 0
    </select>

    <select id="selectUserPermissions" resultType="java.lang.String">
        SELECT DISTINCT m.permission FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.id = rm.menu_id
        INNER JOIN sys_user_role ur ON rm.role_id = ur.role_id
        WHERE ur.user_id = #{userId} AND m.permission IS NOT NULL AND m.permission != '' AND m.status = 1
    </select>

</mapper>
