# P0.2 数据脱敏实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 通过注解驱动的 Jackson 序列化器实现 API 响应数据自动脱敏，管理员角色可查看原文。

**Architecture:** 自定义 `@Sensitive` 注解 + `SensitiveSerializer` Jackson 序列化器，在 JSON 序列化时自动对标记字段执行脱敏规则。通过 SecurityContext 判断当前用户角色决定是否跳过脱敏。零侵入现有业务代码。

**Tech Stack:** Jackson, Spring Security, 自定义注解

---

## File Structure

### Backend (New Files)
- `admin-backend/src/main/java/com/iflytek/admin/common/enums/SensitiveType.java` — 脱敏类型枚举
- `admin-backend/src/main/java/com/iflytek/admin/common/annotation/Sensitive.java` — 脱敏注解
- `admin-backend/src/main/java/com/iflytek/admin/common/serializer/SensitiveSerializer.java` — Jackson 序列化器

### Backend (Modified Files)
- `admin-backend/src/main/java/com/iflytek/admin/modules/system/entity/SysUser.java` — 标注 phone、email

---

## Task 1: 脱敏类型枚举

**Files:**
- Create: `admin-backend/src/main/java/com/iflytek/admin/common/enums/SensitiveType.java`

- [ ] **Step 1: 创建 SensitiveType 枚举**

```java
package com.iflytek.admin.common.enums;

import java.util.function.Function;

public enum SensitiveType {

    /** 手机号: 138****5678 */
    PHONE(val -> {
        if (val == null || val.length() < 7) return val;
        return val.substring(0, 3) + "****" + val.substring(val.length() - 4);
    }),

    /** 邮箱: z***@example.com */
    EMAIL(val -> {
        if (val == null || !val.contains("@")) return val;
        int atIdx = val.indexOf('@');
        if (atIdx <= 1) return val;
        return val.charAt(0) + "***" + val.substring(atIdx);
    }),

    /** 身份证: 320***********1234 */
    ID_CARD(val -> {
        if (val == null || val.length() < 8) return val;
        return val.substring(0, 3) + "*".repeat(val.length() - 7) + val.substring(val.length() - 4);
    }),

    /** 银行卡: ************1234 */
    BANK_CARD(val -> {
        if (val == null || val.length() < 4) return val;
        return "*".repeat(val.length() - 4) + val.substring(val.length() - 4);
    }),

    /** 地址: 保留前6个字符 */
    ADDRESS(val -> {
        if (val == null || val.length() <= 6) return val;
        return val.substring(0, 6) + "***";
    });

    private final Function<String, String> maskFunction;

    SensitiveType(Function<String, String> maskFunction) {
        this.maskFunction = maskFunction;
    }

    public String mask(String value) {
        return maskFunction.apply(value);
    }
}
```

- [ ] **Step 2: 验证编译**

Run: `cd /Users/yili/Desktop/iflytek/test-demo/code/admin-backend && mvn compile -q`
Expected: BUILD SUCCESS

- [ ] **Step 3: Commit**

```bash
git add admin-backend/src/main/java/com/iflytek/admin/common/enums/SensitiveType.java
git commit -m "feat(masking): add SensitiveType enum with mask functions"
```

---

## Task 2: @Sensitive 注解和序列化器

**Files:**
- Create: `admin-backend/src/main/java/com/iflytek/admin/common/annotation/Sensitive.java`
- Create: `admin-backend/src/main/java/com/iflytek/admin/common/serializer/SensitiveSerializer.java`

- [ ] **Step 1: 创建 @Sensitive 注解**

```java
package com.iflytek.admin.common.annotation;

import com.fasterxml.jackson.annotation.JacksonAnnotationsInside;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.iflytek.admin.common.enums.SensitiveType;
import com.iflytek.admin.common.serializer.SensitiveSerializer;

import java.lang.annotation.*;

@Target(ElementType.FIELD)
@Retention(RetentionPolicy.RUNTIME)
@Documented
@JacksonAnnotationsInside
@JsonSerialize(using = SensitiveSerializer.class)
public @interface Sensitive {
    SensitiveType value();
}
```

- [ ] **Step 2: 创建 SensitiveSerializer**

```java
package com.iflytek.admin.common.serializer;

import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.databind.BeanProperty;
import com.fasterxml.jackson.databind.JsonMappingException;
import com.fasterxml.jackson.databind.JsonSerializer;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.ser.ContextualSerializer;
import com.iflytek.admin.common.annotation.Sensitive;
import com.iflytek.admin.common.enums.SensitiveType;
import org.springframework.security.core.Authentication;
import org.springframework.security.core.GrantedAuthority;
import org.springframework.security.core.context.SecurityContextHolder;

import java.io.IOException;

public class SensitiveSerializer extends JsonSerializer<String> implements ContextualSerializer {

    private SensitiveType sensitiveType;

    public SensitiveSerializer() {
    }

    public SensitiveSerializer(SensitiveType sensitiveType) {
        this.sensitiveType = sensitiveType;
    }

    @Override
    public void serialize(String value, JsonGenerator gen, SerializerProvider serializers) throws IOException {
        if (value == null) {
            gen.writeNull();
            return;
        }
        // 管理员角色跳过脱敏
        if (isAdmin()) {
            gen.writeString(value);
            return;
        }
        gen.writeString(sensitiveType.mask(value));
    }

    @Override
    public JsonSerializer<?> createContextual(SerializerProvider prov, BeanProperty property) throws JsonMappingException {
        if (property == null) {
            return this;
        }
        Sensitive annotation = property.getAnnotation(Sensitive.class);
        if (annotation == null) {
            annotation = property.getContextAnnotation(Sensitive.class);
        }
        if (annotation != null) {
            return new SensitiveSerializer(annotation.value());
        }
        return prov.findValueSerializer(property.getType(), property);
    }

    private boolean isAdmin() {
        Authentication auth = SecurityContextHolder.getContext().getAuthentication();
        if (auth == null) return false;
        return auth.getAuthorities().stream()
                .map(GrantedAuthority::getAuthority)
                .anyMatch(a -> a.equals("admin") || a.equals("ROLE_admin"));
    }
}
```

- [ ] **Step 3: 验证编译**

Run: `cd /Users/yili/Desktop/iflytek/test-demo/code/admin-backend && mvn compile -q`
Expected: BUILD SUCCESS

- [ ] **Step 4: Commit**

```bash
git add admin-backend/src/main/java/com/iflytek/admin/common/annotation/Sensitive.java
git add admin-backend/src/main/java/com/iflytek/admin/common/serializer/SensitiveSerializer.java
git commit -m "feat(masking): add @Sensitive annotation and SensitiveSerializer"
```

---

## Task 3: 应用脱敏注解到实体

**Files:**
- Modify: `admin-backend/src/main/java/com/iflytek/admin/modules/system/entity/SysUser.java`

- [ ] **Step 1: 在 SysUser 实体字段上添加 @Sensitive 注解**

添加 import：
```java
import com.iflytek.admin.common.annotation.Sensitive;
import com.iflytek.admin.common.enums.SensitiveType;
```

修改 phone 和 email 字段：
```java
@Sensitive(SensitiveType.EMAIL)
private String email;

@Sensitive(SensitiveType.PHONE)
private String phone;
```

- [ ] **Step 2: 验证编译**

Run: `cd /Users/yili/Desktop/iflytek/test-demo/code/admin-backend && mvn compile -q`
Expected: BUILD SUCCESS

- [ ] **Step 3: Commit**

```bash
git add admin-backend/src/main/java/com/iflytek/admin/modules/system/entity/SysUser.java
git commit -m "feat(masking): apply @Sensitive to SysUser phone and email fields"
```
