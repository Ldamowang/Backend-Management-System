# P0.5 通用数据导入导出实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建通用的导入导出框架，支持任意列表页一键导出 Excel/CSV，以及模板下载 → 上传 → 预校验 → 确认导入的完整导入流程。

**Architecture:** 后端基于 EasyExcel + `@Exportable` 注解驱动，通用 ExportService/ImportService 处理序列化和校验。前端封装 `useExport`/`useImport` composable 和 `ImportDialog` 组件，实现统一的导入导出交互。

**Tech Stack:** EasyExcel 3.x, Spring Boot, Vue 3, Element Plus

---

## File Structure

### Backend (New Files)
- `admin-backend/src/main/java/com/iflytek/admin/common/annotation/Exportable.java` — 导出字段注解
- `admin-backend/src/main/java/com/iflytek/admin/common/service/ExcelExportService.java` — 通用导出服务
- `admin-backend/src/main/java/com/iflytek/admin/common/service/ExcelImportService.java` — 通用导入服务
- `admin-backend/src/main/java/com/iflytek/admin/common/dto/ImportResult.java` — 导入结果 DTO

### Backend (Modified Files)
- `admin-backend/pom.xml` — 添加 EasyExcel 依赖（替代 POI 直接使用）
- `admin-backend/src/main/java/com/iflytek/admin/modules/system/controller/UserController.java` — 重构导出 + 增加导入

### Frontend (New Files)
- `admin-frontend/src/composables/useExport.ts` — 导出 composable
- `admin-frontend/src/composables/useImport.ts` — 导入 composable
- `admin-frontend/src/components/ImportDialog.vue` — 通用导入对话框

### Frontend (Modified Files)
- `admin-frontend/src/views/system/user/index.vue` — 使用新的导入导出 composable

---

## Task 1: 后端依赖和注解

**Files:**
- Modify: `admin-backend/pom.xml`
- Create: `admin-backend/src/main/java/com/iflytek/admin/common/annotation/Exportable.java`

- [ ] **Step 1: 添加 EasyExcel 依赖**

在 `pom.xml` 的 `<dependencies>` 中添加：

```xml
<!-- EasyExcel -->
<dependency>
    <groupId>com.alibaba</groupId>
    <artifactId>easyexcel</artifactId>
    <version>3.3.4</version>
</dependency>
```

- [ ] **Step 2: 创建 @Exportable 注解**

```java
package com.iflytek.admin.common.annotation;

import java.lang.annotation.*;

/**
 * 标记可导出/导入的字段
 */
@Target(ElementType.FIELD)
@Retention(RetentionPolicy.RUNTIME)
@Documented
public @interface Exportable {
    /** 列标题 */
    String value();

    /** 列序号（越小越靠前） */
    int order() default 0;

    /** 是否参与导入 */
    boolean importable() default true;

    /** 导入时是否必填 */
    boolean required() default false;
}
```

- [ ] **Step 3: 创建 ImportResult DTO**

```java
package com.iflytek.admin.common.dto;

import lombok.Builder;
import lombok.Data;

import java.util.ArrayList;
import java.util.List;

@Data
@Builder
public class ImportResult {
    private int totalCount;
    private int successCount;
    private int failCount;
    @Builder.Default
    private List<ErrorDetail> errors = new ArrayList<>();

    @Data
    @Builder
    public static class ErrorDetail {
        private int row;
        private String field;
        private String message;
    }
}
```

- [ ] **Step 4: 验证编译**

Run: `cd /Users/yili/Desktop/iflytek/test-demo/code/admin-backend && mvn compile -q`
Expected: BUILD SUCCESS

- [ ] **Step 5: Commit**

```bash
git add admin-backend/pom.xml
git add admin-backend/src/main/java/com/iflytek/admin/common/annotation/Exportable.java
git add admin-backend/src/main/java/com/iflytek/admin/common/dto/ImportResult.java
git commit -m "feat(excel): add EasyExcel dependency, @Exportable annotation and ImportResult DTO"
```

---

## Task 2: 通用导出服务

**Files:**
- Create: `admin-backend/src/main/java/com/iflytek/admin/common/service/ExcelExportService.java`

- [ ] **Step 1: 创建 ExcelExportService**

```java
package com.iflytek.admin.common.service;

import com.alibaba.excel.EasyExcel;
import com.alibaba.excel.write.style.column.LongestMatchColumnWidthStyleStrategy;
import com.iflytek.admin.common.annotation.Exportable;
import jakarta.servlet.http.HttpServletResponse;
import org.springframework.stereotype.Service;

import java.io.IOException;
import java.lang.reflect.Field;
import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.*;
import java.util.stream.Collectors;

@Service
public class ExcelExportService {

    /**
     * 导出数据到 Excel
     *
     * @param response HTTP 响应
     * @param fileName 文件名（不含扩展名）
     * @param data     数据列表
     * @param clazz    数据类（需要 @Exportable 注解的字段）
     */
    public <T> void export(HttpServletResponse response, String fileName, List<T> data, Class<T> clazz) throws IOException {
        List<Field> fields = getExportableFields(clazz);
        List<List<String>> headers = fields.stream()
                .map(f -> Collections.singletonList(f.getAnnotation(Exportable.class).value()))
                .collect(Collectors.toList());

        List<List<Object>> rows = new ArrayList<>();
        for (T item : data) {
            List<Object> row = new ArrayList<>();
            for (Field field : fields) {
                field.setAccessible(true);
                try {
                    row.add(field.get(item));
                } catch (IllegalAccessException e) {
                    row.add("");
                }
            }
            rows.add(row);
        }

        response.setContentType("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet");
        response.setHeader("Content-Disposition", "attachment;filename=" +
                URLEncoder.encode(fileName + ".xlsx", StandardCharsets.UTF_8));
        response.setHeader("Access-Control-Expose-Headers", "Content-Disposition");

        EasyExcel.write(response.getOutputStream())
                .head(headers)
                .registerWriteHandler(new LongestMatchColumnWidthStyleStrategy())
                .sheet("Sheet1")
                .doWrite(rows);
    }

    /**
     * 导出空模板（用于导入）
     */
    public <T> void exportTemplate(HttpServletResponse response, String fileName, Class<T> clazz) throws IOException {
        export(response, fileName + "_导入模板", Collections.emptyList(), clazz);
    }

    private List<Field> getExportableFields(Class<?> clazz) {
        List<Field> fields = new ArrayList<>();
        for (Field field : clazz.getDeclaredFields()) {
            if (field.isAnnotationPresent(Exportable.class)) {
                fields.add(field);
            }
        }
        fields.sort(Comparator.comparingInt(f -> f.getAnnotation(Exportable.class).order()));
        return fields;
    }
}
```

- [ ] **Step 2: 验证编译**

Run: `cd /Users/yili/Desktop/iflytek/test-demo/code/admin-backend && mvn compile -q`
Expected: BUILD SUCCESS

- [ ] **Step 3: Commit**

```bash
git add admin-backend/src/main/java/com/iflytek/admin/common/service/ExcelExportService.java
git commit -m "feat(excel): add generic ExcelExportService"
```

---

## Task 3: 通用导入服务

**Files:**
- Create: `admin-backend/src/main/java/com/iflytek/admin/common/service/ExcelImportService.java`

- [ ] **Step 1: 创建 ExcelImportService**

```java
package com.iflytek.admin.common.service;

import com.alibaba.excel.EasyExcel;
import com.alibaba.excel.context.AnalysisContext;
import com.alibaba.excel.read.listener.ReadListener;
import com.iflytek.admin.common.annotation.Exportable;
import com.iflytek.admin.common.dto.ImportResult;
import org.springframework.stereotype.Service;
import org.springframework.web.multipart.MultipartFile;

import java.io.IOException;
import java.lang.reflect.Field;
import java.util.*;
import java.util.function.Consumer;
import java.util.function.Function;

@Service
public class ExcelImportService {

    /**
     * 导入 Excel 数据
     *
     * @param file      上传的文件
     * @param clazz     目标类型
     * @param validator 行数据校验函数，返回 null 表示通过，否则返回错误信息
     * @param saver     保存函数，批量保存校验通过的数据
     * @return 导入结果
     */
    public <T> ImportResult importData(
            MultipartFile file,
            Class<T> clazz,
            Function<T, String> validator,
            Consumer<List<T>> saver
    ) throws IOException {
        List<Field> importableFields = getImportableFields(clazz);
        List<T> validRows = new ArrayList<>();
        List<ImportResult.ErrorDetail> errors = new ArrayList<>();
        int[] rowCount = {0};

        EasyExcel.read(file.getInputStream(), new ReadListener<Map<Integer, String>>() {
            @Override
            public void invoke(Map<Integer, String> rowData, AnalysisContext context) {
                rowCount[0]++;
                int rowNum = context.readRowHolder().getRowIndex() + 1;

                try {
                    T obj = clazz.getDeclaredConstructor().newInstance();
                    for (int i = 0; i < importableFields.size(); i++) {
                        Field field = importableFields.get(i);
                        field.setAccessible(true);
                        String cellValue = rowData.get(i);

                        // 必填校验
                        Exportable anno = field.getAnnotation(Exportable.class);
                        if (anno.required() && (cellValue == null || cellValue.isBlank())) {
                            errors.add(ImportResult.ErrorDetail.builder()
                                    .row(rowNum)
                                    .field(anno.value())
                                    .message("不能为空")
                                    .build());
                            return;
                        }

                        if (cellValue != null && !cellValue.isBlank()) {
                            setFieldValue(field, obj, cellValue);
                        }
                    }

                    // 自定义校验
                    String error = validator.apply(obj);
                    if (error != null) {
                        errors.add(ImportResult.ErrorDetail.builder()
                                .row(rowNum)
                                .field("")
                                .message(error)
                                .build());
                        return;
                    }

                    validRows.add(obj);
                } catch (Exception e) {
                    errors.add(ImportResult.ErrorDetail.builder()
                            .row(rowNum)
                            .field("")
                            .message("数据解析失败: " + e.getMessage())
                            .build());
                }
            }

            @Override
            public void doAfterAllAnalysed(AnalysisContext context) {
                // 批量保存有效数据
                if (!validRows.isEmpty()) {
                    saver.accept(validRows);
                }
            }
        }).headRowNumber(1).sheet().doRead();

        return ImportResult.builder()
                .totalCount(rowCount[0])
                .successCount(validRows.size())
                .failCount(errors.size())
                .errors(errors)
                .build();
    }

    private void setFieldValue(Field field, Object obj, String value) throws IllegalAccessException {
        Class<?> type = field.getType();
        if (type == String.class) {
            field.set(obj, value);
        } else if (type == Integer.class || type == int.class) {
            field.set(obj, Integer.parseInt(value));
        } else if (type == Long.class || type == long.class) {
            field.set(obj, Long.parseLong(value));
        } else if (type == Double.class || type == double.class) {
            field.set(obj, Double.parseDouble(value));
        } else {
            field.set(obj, value);
        }
    }

    private List<Field> getImportableFields(Class<?> clazz) {
        List<Field> fields = new ArrayList<>();
        for (Field field : clazz.getDeclaredFields()) {
            Exportable anno = field.getAnnotation(Exportable.class);
            if (anno != null && anno.importable()) {
                fields.add(field);
            }
        }
        fields.sort(Comparator.comparingInt(f -> f.getAnnotation(Exportable.class).order()));
        return fields;
    }
}
```

- [ ] **Step 2: 验证编译**

Run: `cd /Users/yili/Desktop/iflytek/test-demo/code/admin-backend && mvn compile -q`
Expected: BUILD SUCCESS

- [ ] **Step 3: Commit**

```bash
git add admin-backend/src/main/java/com/iflytek/admin/common/service/ExcelImportService.java
git commit -m "feat(excel): add generic ExcelImportService with validation pipeline"
```

---

## Task 4: 用户管理应用导入导出

**Files:**
- Modify: `admin-backend/src/main/java/com/iflytek/admin/modules/system/controller/UserController.java`

- [ ] **Step 1: 创建用户导出 DTO**

新建 `admin-backend/src/main/java/com/iflytek/admin/modules/system/dto/UserExportDTO.java`：

```java
package com.iflytek.admin.modules.system.dto;

import com.iflytek.admin.common.annotation.Exportable;
import lombok.Data;

@Data
public class UserExportDTO {
    @Exportable(value = "用户名", order = 1, required = true)
    private String username;

    @Exportable(value = "昵称", order = 2, required = true)
    private String nickname;

    @Exportable(value = "邮箱", order = 3)
    private String email;

    @Exportable(value = "手机号", order = 4)
    private String phone;

    @Exportable(value = "性别(0女1男2未知)", order = 5)
    private Integer gender;

    @Exportable(value = "状态(0禁用1正常)", order = 6, importable = false)
    private Integer status;
}
```

- [ ] **Step 2: 重构 UserController 导出方法并添加导入**

注入 ExcelExportService 和 ExcelImportService：

```java
private final ExcelExportService excelExportService;
private final ExcelImportService excelImportService;
```

重构导出方法：
```java
@Log(module = "用户管理", operation = "导出用户")
@GetMapping("/export")
@PreAuthorize("hasAuthority('sys:user:export')")
public void export(UserQueryDTO query, HttpServletResponse response) throws IOException {
    List<SysUser> users = userService.listForExport(query);
    List<UserExportDTO> exportList = users.stream().map(u -> {
        UserExportDTO dto = new UserExportDTO();
        dto.setUsername(u.getUsername());
        dto.setNickname(u.getNickname());
        dto.setEmail(u.getEmail());
        dto.setPhone(u.getPhone());
        dto.setGender(u.getGender());
        dto.setStatus(u.getStatus());
        return dto;
    }).toList();
    excelExportService.export(response, "用户列表", exportList, UserExportDTO.class);
}

@GetMapping("/import-template")
@PreAuthorize("hasAuthority('sys:user:add')")
public void importTemplate(HttpServletResponse response) throws IOException {
    excelExportService.exportTemplate(response, "用户", UserExportDTO.class);
}

@Log(module = "用户管理", operation = "导入用户")
@PostMapping("/import")
@PreAuthorize("hasAuthority('sys:user:add')")
public Result<ImportResult> importUsers(@RequestParam("file") MultipartFile file) throws IOException {
    ImportResult result = excelImportService.importData(
            file,
            UserExportDTO.class,
            dto -> {
                // 校验用户名唯一性
                if (userService.existsByUsername(dto.getUsername())) {
                    return "用户名 " + dto.getUsername() + " 已存在";
                }
                return null;
            },
            validList -> {
                userService.batchImport(validList);
            }
    );
    return Result.success(result);
}
```

- [ ] **Step 3: 验证编译**

Run: `cd /Users/yili/Desktop/iflytek/test-demo/code/admin-backend && mvn compile -q`
Expected: BUILD SUCCESS

- [ ] **Step 4: Commit**

```bash
git add admin-backend/src/main/java/com/iflytek/admin/modules/system/dto/UserExportDTO.java
git add admin-backend/src/main/java/com/iflytek/admin/modules/system/controller/UserController.java
git commit -m "feat(excel): apply import/export framework to user management"
```

---

## Task 5: 前端导出 Composable

**Files:**
- Create: `admin-frontend/src/composables/useExport.ts`

- [ ] **Step 1: 创建 useExport**

```typescript
import { ref } from 'vue'
import { ElMessage } from 'element-plus'
import { getToken } from '@/utils/auth'

export function useExport() {
  const exporting = ref(false)

  const exportData = async (url: string, params?: Record<string, any>, fileName?: string) => {
    exporting.value = true
    try {
      const query = params ? '?' + new URLSearchParams(
        Object.entries(params)
          .filter(([, v]) => v !== undefined && v !== null && v !== '')
          .map(([k, v]) => [k, String(v)])
      ).toString() : ''

      const response = await fetch(`${import.meta.env.VITE_API_BASE_URL}${url}${query}`, {
        headers: {
          Authorization: `Bearer ${getToken()}`
        }
      })

      if (!response.ok) {
        throw new Error('导出失败')
      }

      const blob = await response.blob()
      const downloadUrl = window.URL.createObjectURL(blob)
      const link = document.createElement('a')
      link.href = downloadUrl

      // 从 Content-Disposition 获取文件名
      const disposition = response.headers.get('Content-Disposition')
      const defaultName = fileName || 'export'
      link.download = disposition
        ? decodeURIComponent(disposition.split('filename=')[1] || defaultName + '.xlsx')
        : defaultName + '.xlsx'

      document.body.appendChild(link)
      link.click()
      document.body.removeChild(link)
      window.URL.revokeObjectURL(downloadUrl)

      ElMessage.success('导出成功')
    } catch (error: any) {
      ElMessage.error(error.message || '导出失败')
    } finally {
      exporting.value = false
    }
  }

  const downloadTemplate = (url: string, fileName?: string) => {
    return exportData(url, undefined, fileName || '导入模板')
  }

  return { exporting, exportData, downloadTemplate }
}
```

- [ ] **Step 2: Commit**

```bash
git add admin-frontend/src/composables/useExport.ts
git commit -m "feat(excel): add useExport composable for frontend export"
```

---

## Task 6: 前端导入 Composable 和对话框

**Files:**
- Create: `admin-frontend/src/composables/useImport.ts`
- Create: `admin-frontend/src/components/ImportDialog.vue`

- [ ] **Step 1: 创建 useImport**

```typescript
import { ref } from 'vue'
import request from '@/api/request'

export interface ImportResultData {
  totalCount: number
  successCount: number
  failCount: number
  errors: { row: number; field: string; message: string }[]
}

export function useImport() {
  const importing = ref(false)
  const importResult = ref<ImportResultData | null>(null)

  const importData = async (url: string, file: File): Promise<ImportResultData> => {
    importing.value = true
    importResult.value = null
    try {
      const formData = new FormData()
      formData.append('file', file)
      const res = await request.post<any, ImportResultData>(url, formData, {
        headers: { 'Content-Type': 'multipart/form-data' }
      })
      importResult.value = res
      return res
    } finally {
      importing.value = false
    }
  }

  return { importing, importResult, importData }
}
```

- [ ] **Step 2: 创建 ImportDialog 组件**

```vue
<template>
  <el-dialog v-model="visible" title="数据导入" width="500px" @close="handleClose">
    <!-- 上传区域 -->
    <div v-if="!result">
      <el-upload
        ref="uploadRef"
        drag
        :auto-upload="false"
        :limit="1"
        accept=".xlsx,.xls,.csv"
        :on-change="handleFileChange"
      >
        <el-icon :size="40"><UploadFilled /></el-icon>
        <div>将文件拖到此处，或<em>点击上传</em></div>
        <template #tip>
          <div class="el-upload__tip">
            仅支持 .xlsx / .xls / .csv 文件
            <el-button type="primary" link @click="$emit('download-template')">下载导入模板</el-button>
          </div>
        </template>
      </el-upload>
    </div>

    <!-- 导入结果 -->
    <div v-else>
      <el-result
        :icon="result.failCount === 0 ? 'success' : 'warning'"
        :title="`导入完成：成功 ${result.successCount} 条，失败 ${result.failCount} 条`"
      />
      <el-table v-if="result.errors.length > 0" :data="result.errors" max-height="300" style="margin-top: 12px">
        <el-table-column label="行号" prop="row" width="70" />
        <el-table-column label="字段" prop="field" width="120" />
        <el-table-column label="错误信息" prop="message" />
      </el-table>
    </div>

    <template #footer>
      <el-button v-if="!result" @click="visible = false">取消</el-button>
      <el-button v-if="!result" type="primary" :loading="importing" :disabled="!selectedFile" @click="handleImport">
        确认导入
      </el-button>
      <el-button v-if="result" type="primary" @click="handleClose">完成</el-button>
    </template>
  </el-dialog>
</template>

<script setup lang="ts">
import { ref, watch } from 'vue'
import { UploadFilled } from '@element-plus/icons-vue'
import { useImport } from '@/composables/useImport'
import type { ImportResultData } from '@/composables/useImport'
import type { UploadFile } from 'element-plus'

const props = defineProps<{
  modelValue: boolean
  importUrl: string
}>()

const emit = defineEmits<{
  'update:modelValue': [value: boolean]
  'download-template': []
  'success': []
}>()

const visible = ref(props.modelValue)
watch(() => props.modelValue, (val) => { visible.value = val })
watch(visible, (val) => { emit('update:modelValue', val) })

const selectedFile = ref<File | null>(null)
const result = ref<ImportResultData | null>(null)
const { importing, importData } = useImport()

const handleFileChange = (file: UploadFile) => {
  selectedFile.value = file.raw || null
}

const handleImport = async () => {
  if (!selectedFile.value) return
  result.value = await importData(props.importUrl, selectedFile.value)
  if (result.value.failCount === 0) {
    emit('success')
  }
}

const handleClose = () => {
  visible.value = false
  selectedFile.value = null
  result.value = null
}
</script>
```

- [ ] **Step 3: 验证类型检查**

Run: `cd /Users/yili/Desktop/iflytek/test-demo/code/admin-frontend && npm run type-check`
Expected: 无类型错误

- [ ] **Step 4: Commit**

```bash
git add admin-frontend/src/composables/useImport.ts
git add admin-frontend/src/components/ImportDialog.vue
git commit -m "feat(excel): add useImport composable and ImportDialog component"
```

---

## Task 7: 用户管理页面集成导入导出

**Files:**
- Modify: `admin-frontend/src/views/system/user/index.vue`

- [ ] **Step 1: 在用户管理页面添加导入导出按钮和组件**

导入依赖：
```typescript
import { useExport } from '@/composables/useExport'
import ImportDialog from '@/components/ImportDialog.vue'

const { exporting, exportData, downloadTemplate } = useExport()
const showImportDialog = ref(false)
```

在工具栏添加按钮（在新增按钮旁边）：
```vue
<el-button v-permission="'sys:user:export'" :loading="exporting" @click="handleExport">导出</el-button>
<el-button v-permission="'sys:user:add'" @click="showImportDialog = true">导入</el-button>
```

添加 ImportDialog：
```vue
<ImportDialog
  v-model="showImportDialog"
  import-url="/users/import"
  @download-template="downloadTemplate('/users/import-template', '用户导入模板')"
  @success="fetchData"
/>
```

导出处理函数：
```typescript
const handleExport = () => {
  exportData('/users/export', queryParams)
}
```

- [ ] **Step 2: 验证类型检查**

Run: `cd /Users/yili/Desktop/iflytek/test-demo/code/admin-frontend && npm run type-check`
Expected: 无类型错误

- [ ] **Step 3: Commit**

```bash
git add admin-frontend/src/views/system/user/index.vue
git commit -m "feat(excel): integrate import/export into user management page"
```
